apiVersion: apps/v1
kind: Deployment
metadata:
  name: collector-loadbalancer
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: opentelemetry-collector
      app.kubernetes.io/instance: collector-loadbalancer
  template:
    metadata:
      labels:
        app.kubernetes.io/name: opentelemetry-collector
        app.kubernetes.io/instance: collector-loadbalancer
    spec:
      serviceAccountName: collector-loadbalancer
      containers:
        - name: opentelemetry-collector
          args:
            - --config=/conf/relay.yaml
          image: otel/opentelemetry-collector-contrib:latest
          ports:
            - name: otlp
              containerPort: 4317
              protocol: TCP
          env:
            - name: MY_POD_IP
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: status.podIP
          livenessProbe:
            httpGet:
              path: /
              port: 13133
            initialDelaySeconds: 3
          readinessProbe:
            httpGet:
              path: /
              port: 13133
            initialDelaySeconds: 3
          resources:
            limits:
              cpu: 128m
              memory: 256Mi
          volumeMounts:
            - mountPath: /conf
              name: opentelemetry-collector-configmap
      volumes:
        - name: opentelemetry-collector-configmap
          configMap:
            name: collector-loadbalancer-config
            items:
              - key: relay
                path: relay.yaml
