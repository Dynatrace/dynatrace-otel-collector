apiVersion: apps/v1
kind: Deployment
metadata:
  name: collector-receiver4
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: collector-receiver4
      app.kubernetes.io/instance: collector-receiver4
  template:
    metadata:
      labels:
        app.kubernetes.io/name: collector-receiver4
        app.kubernetes.io/instance: collector-receiver4
    spec:
      serviceAccountName: collector-receiver4
      containers:
        - name: collector-receiver4
          args:
            - --config=/conf/relay.yaml
          image: otel/opentelemetry-collector-contrib:latest
          ports:
            - name: otlp
              containerPort: 4317
              protocol: TCP
          env:
            - name: MY_POD_IP
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: status.podIP
          livenessProbe:
            httpGet:
              path: /
              port: 13133
            initialDelaySeconds: 3
          readinessProbe:
            httpGet:
              path: /
              port: 13133
            initialDelaySeconds: 3
          resources:
            limits:
              cpu: 128m
              memory: 256Mi
          volumeMounts:
            - mountPath: /conf
              name: collector-receiver4-configmap
      volumes:
        - name: collector-receiver4-configmap
          configMap:
            name: collector-receiver4-config
            items:
              - key: relay
                path: relay.yaml
