exporters:
  logging:
    verbosity: detailed
    sampling_initial: 5
    sampling_thereafter: 200

receivers:
  otlp:
    protocols:
      grpc:
      http:

service:
  pipelines:
    metrics:
      receivers: [otlp]
      processors: []
      exporters: [logging]