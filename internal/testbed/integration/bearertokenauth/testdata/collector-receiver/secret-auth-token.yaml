# Holds the expected Bearer token value the collector will validate against.
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Name }}-auth-token
  namespace: e2ebearertokenauth
type: Opaque
stringData:
  auth-token: "e2etestbearertoken"
