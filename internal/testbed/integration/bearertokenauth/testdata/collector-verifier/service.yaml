apiVersion: v1
kind: Service
metadata:
  name: {{ .Name }}
  namespace: e2ebearertokenauth
spec:
  type: ClusterIP
  ports:
    - name: otlp-http
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app.kubernetes.io/name: opentelemetry-collector
    app.kubernetes.io/instance: {{ .Name }}
