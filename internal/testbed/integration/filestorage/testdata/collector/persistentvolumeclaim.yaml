apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: filestorage-pvc-{{ .Name }}
  namespace: e2e-filestorage
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: filestorage-local
  volumeName: filestorage-pv-{{ .Name }}
