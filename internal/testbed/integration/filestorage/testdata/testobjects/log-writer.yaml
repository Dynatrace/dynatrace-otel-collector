apiVersion: apps/v1
kind: Deployment
metadata:
  name: log-writer
  namespace: e2e-filestorage
spec:
  replicas: 1
  selector:
    matchLabels:
      app: log-writer
  template:
    metadata:
      labels:
        app: log-writer
    spec:
      containers:
        - name: log-writer
          image: busybox:latest@sha256:dc2d74b28e4cf8984fa52af1f39bc7c3d9c73760b41a74d629f5d11b1ab28616
          imagePullPolicy: IfNotPresent
          command:
            - /bin/sh
            - -c
            - |
              mkdir -p /var/log/app
              counter=1
              while true; do
                echo "Log entry ${counter} from filestorage checkpoint test at $(date)" >> /var/log/app/test.log
                counter=$((counter + 1))
                sleep 1
              done
          volumeMounts:
            - name: log-volume
              mountPath: /var/log/app
          resources:
            limits:
              cpu: 50m
              memory: 64Mi
      volumes:
        - name: log-volume
          hostPath:
            path: /var/log/app
            type: DirectoryOrCreate

