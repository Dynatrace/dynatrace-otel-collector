apiVersion: apps/v1
kind: Deployment
metadata:
  name: temeletrygen-metrics-deployment2
  namespace: e2eloadbalancing
spec:
  replicas: 1
  selector:
    matchLabels:
      app: temeletrygen-metrics-deployment2
  template:
    metadata:
      annotations:
        workload: deployment
      labels:
        app: temeletrygen-metrics-deployment2
    spec:
      containers:
      - command:
        - /telemetrygen
        - metrics
        - --otlp-insecure
        - --otlp-metric-name=custom-metric-name2
        - --otlp-endpoint={{ .OTLPEndpoint }}
        - --duration=36000s
        - --rate=1
        - --otlp-attributes=service.name="test-metrics-deployment"
        image: odubajdt/telemetrygen:metric-name-final
        imagePullPolicy: Always
        name: telemetrygen
      restartPolicy: Always
