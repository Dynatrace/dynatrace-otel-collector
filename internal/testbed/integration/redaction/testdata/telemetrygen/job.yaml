apiVersion: batch/v1
kind: Job
metadata:
  name: {{ .Name }}-{{ .DataType}}-job
  namespace: e2eredaction
spec:
  template:
    metadata:
      annotations:
        workload: job
      labels:
        app: {{ .Name }}-{{ .DataType }}-job
    spec:
      containers:
        - command:
            - /telemetrygen
            - {{ .DataType }}
            - --otlp-insecure
            - --otlp-endpoint={{ .OTLPEndpoint }}
            - --rate=1
            - --duration=36000s
            - --otlp-attributes=service.name="test-{{ .DataType }}-job"
          image: ghcr.io/open-telemetry/opentelemetry-collector-contrib/telemetrygen:v0.148.0@sha256:55beefe284b1a3aad3ff9d3a470485042545248be34dea1a3d6a46e2566382d4
          name: telemetrygen
      restartPolicy: OnFailure
