receivers:
  otlp:
    protocols:
      http:
        endpoint: localhost:4318

exporters:
  logging:
    verbosity: detailed

service:
  pipelines:
    metrics:
      receivers: [otlp]
      exporters: [logging]
  telemetry:
    metrics:
      level: normal
      address: localhost:9090
